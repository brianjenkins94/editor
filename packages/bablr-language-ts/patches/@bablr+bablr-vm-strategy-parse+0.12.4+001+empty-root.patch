diff --git a/node_modules/@bablr/bablr-vm-strategy-parse/lib/evaluate.js b/node_modules/@bablr/bablr-vm-strategy-parse/lib/evaluate.js
index 603a726..df2a1c9 100644
--- a/node_modules/@bablr/bablr-vm-strategy-parse/lib/evaluate.js
+++ b/node_modules/@bablr/bablr-vm-strategy-parse/lib/evaluate.js
@@ -648,7 +648,10 @@ function* parseStrategy(ctx, getState, rootMatcher, rootValue) {
       let emptyCover = false;
 
       let failing =
-        isThrowing || emptyCover || (!allowEmpty && isEmpty_) || (literalMatcher && !literalValue);
+        isThrowing ||
+        emptyCover ||
+        (!allowEmpty && isEmpty_ && !(isCover && finishedMatch.depth === 0)) || // the root cover <_> has no production name so emptyables can never apply; let the covered node decide
+        (literalMatcher && !literalValue);
       throwing = failing && !shouldBranch(effects);
 
       if (failing) {
